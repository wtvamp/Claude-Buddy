<Window x:Class="ClaudeBuddy.OrbWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Buddy"
        Width="56" Height="56"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Reset this session to idle" Click="ResetIdle_Click"/>
                <MenuItem Header="Exit Claude Buddy" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <!-- Soft glow behind the orb -->
        <Ellipse x:Name="Glow"
                 Width="56" Height="56"
                 Fill="{Binding ElementName=Orb, Path=Fill}"
                 Opacity="0.35">
            <Ellipse.Effect>
                <BlurEffect Radius="18"/>
            </Ellipse.Effect>
        </Ellipse>

        <!-- The buddy itself -->
        <Ellipse x:Name="Orb"
                 Width="36" Height="36"
                 Stroke="#22FFFFFF"
                 StrokeThickness="1">
            <Ellipse.Fill>
                <SolidColorBrush x:Name="OrbBrush" Color="#5B7A94"/>
            </Ellipse.Fill>
            <Ellipse.RenderTransform>
                <ScaleTransform x:Name="OrbScale" CenterX="18" CenterY="18" ScaleX="1" ScaleY="1"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <!-- Small glyph so it reads as "something", not just a dot -->
        <TextBlock x:Name="Glyph"
                   Text="•"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#DDFFFFFF"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,-4,0,0"
                   IsHitTestVisible="False"/>
    </Grid>
</Window>
